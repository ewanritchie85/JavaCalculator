A very simple calculator made in Java to get used to Java syntax, as well as OOP projects.

The calculator requests two numbers - integer or floating point - and allows the user
the choice of +, -, *, / and % operations